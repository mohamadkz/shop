# front — React route viewer

Quick static frontend that lists the Laravel routes found in the project.

Commands:

```bash
cd front
npm install
npm run dev
```

The app is a small Vite + React SPA that reads `src/routes.json` and displays grouped routes.
